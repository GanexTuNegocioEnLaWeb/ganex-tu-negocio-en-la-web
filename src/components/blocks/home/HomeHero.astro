---
import { t } from "../../../lib/i18n";
import Header from "../../Header.astro";
import MarketingCard from "./MarketingCard.vue";
const { i18n = {}, lang = "es" } = Astro.props as {
  i18n?: Record<string, any>;
  lang?: string;
};
const tag = t("home.hero.tag", i18n);
const titlePre = t("home.hero.title_pre", i18n);
const titleHighlight = t("home.hero.title_highlight", i18n);
const desc = t("home.hero.desc", i18n);
---

<div class="hero-bg relative pb-32">
  <Header
    lang={lang}
    slug="home"
    variant="transparent"
    class="absolute top-0 w-full z-50"
  />
  <section
    class="max-w-6xl mx-auto w-full flex flex-col lg:flex-row
         items-center justify-between gap-12 lg:gap-16 px-6 py-8 sm:p-8 lg:px-12 lg:py-16"
  >
    <!-- Texto -->
    <article
      class="text-white flex flex-col gap-6 items-start
            pt-0 sm:pt-0 text-center lg:text-left w-full lg:w-1/2"
    >
      <div class="flex flex-col items-center lg:items-start gap-3 mx-auto lg:mx-0">
        <span
          class="text-emerald-300 font-semibold tracking-wider text-sm uppercase bg-emerald-900/40 px-4 py-1.5 rounded-full w-fit border border-emerald-500/40 backdrop-blur-sm"
        >
          {tag}
        </span>
        <h1
          class="text-4xl sm:text-5xl lg:text-6xl
               font-extrabold max-w-2xl leading-tight tracking-tight text-white"
        >
          {titlePre}
          <span
            class="text-transparent bg-clip-text bg-linear-to-r from-emerald-200 to-cyan-200"
            >{titleHighlight}</span
          >
        </h1>
      </div>

      <p
        class="text-base sm:text-lg text-neutral-100 max-w-xl leading-relaxed font-light mx-auto lg:mx-0 opacity-90"
      >
        {desc}
      </p>
    </article>

    <!-- Marketing Card -->
    <MarketingCard client:load lang={lang} i18n={i18n} />
  </section>
  <div
    class="absolute bottom-0 left-0 w-full overflow-hidden leading-none rotate-180"
  >
    <svg
      class="relative block w-[calc(100%+1.3px)] h-25 sm:h-37.5"
      data-name="Layer 1"
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 1200 120"
      preserveAspectRatio="none"
    >
      <path
        d="M321.39,56.44c58-10.79,114.16-30.13,172-41.86,82.39-16.72,168.19-17.73,250.45-.39C823.78,31,906.67,72,985.66,92.83c70.05,18.48,146.53,26.09,214.34,3V0H0V27.35A600.21,600.21,0,0,0,321.39,56.44Z"
        fill="#fafafa"></path>
    </svg>
  </div>
</div>

<style>
  .hero-bg {
    background: linear-gradient(135deg, #ff8a00 0%, #ffb25a 100%);
  }
</style>
