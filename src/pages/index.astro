---
export const prerender = false;

const acceptLanguage = Astro.request.headers.get("accept-language") || "";
const preferredLanguage = acceptLanguage.toLowerCase().includes("en")
    ? "en"
    : "es";

return Astro.redirect(`/${preferredLanguage}`, 307);
---
